{
	"_comment": "Golden snapshot extracted from VS Code Insiders Copilot Chat extension v0.39.2026030604 (dist/extension.js). 6 MCP tools with their input schemas and descriptions. Our server may register extra tools (e.g. read_file) — the compatibility check verifies VS Code's tools are present, not that we have no extras.",
	"tools": [
		{
			"name": "get_vscode_info",
			"description": "Get information about the current VS Code instance",
			"parameters": {}
		},
		{
			"name": "get_selection",
			"description": "Get text selection. Returns current selection if an editor is active, otherwise returns the latest cached selection. The \"current\" field indicates if this is from the active editor (true) or cached (false).",
			"parameters": {}
		},
		{
			"name": "get_diagnostics",
			"description": "Gets language diagnostics (errors, warnings, hints) from VS Code",
			"parameters": {
				"uri": "string"
			},
			"_comment_uri": "uri is optional in VS Code (c_.string().optional()). Omitting it returns diagnostics for all files."
		},
		{
			"name": "open_diff",
			"description": "Opens a diff view comparing original file content with new content. Blocks until user accepts, rejects, or closes the diff.",
			"parameters": {
				"original_file_path": "string",
				"new_file_contents": "string",
				"tab_name": "string"
			}
		},
		{
			"name": "close_diff",
			"description": "Closes a diff tab by its tab name. Use this when the client rejects an edit to close the corresponding diff view.",
			"parameters": {
				"tab_name": "string"
			}
		},
		{
			"name": "update_session_name",
			"description": "Update the display name for the current CLI session",
			"parameters": {
				"name": "string"
			}
		}
	]
}
